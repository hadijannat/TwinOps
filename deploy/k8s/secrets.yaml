apiVersion: v1
kind: Secret
metadata:
  name: twinops-secrets
  namespace: twinops
  labels:
    app.kubernetes.io/name: twinops
type: Opaque
stringData:
  # Set these values for LLM provider authentication
  # Use kubectl create secret or external secrets management in production
  TWINOPS_ANTHROPIC_API_KEY: ""
  TWINOPS_OPENAI_API_KEY: ""
